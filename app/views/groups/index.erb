  <!-- Page Content -->
  <div class="container bg_gray">

    <!-- Page Heading -->
    <h1 class="my-4 pagetitle">グループ一覧
    </h1>

    <% @groups.each do |group| %>
        <a href="groups/<%= group.id %>" class="group-item">
          <div class="group-flex">
            <%=imageIcon group%>
            <div class="group-info">
              <p class="group-name">
                ○○グループ
              </p>
        <p class="tag-box">
          大人数
        </p>
        <p class="group-intro">
          <%= group.description %>
        </p>
            </div>
          </div>
        </a>
  <% end %>
  </div>